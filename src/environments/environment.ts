// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCpsoMP9XBSOm5t-jBz7m0rhKch24ZvaBM",
    authDomain: "mdcu-research-club-database.firebaseapp.com",
    databaseURL: "https://mdcu-research-club-database.firebaseio.com",
    projectId: "mdcu-research-club-database",
    storageBucket: "mdcu-research-club-database.appspot.com",
    messagingSenderId: "420382824938",
    appId: "1:420382824938:web:7d5f6c75d56b6f6b25fa93"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
